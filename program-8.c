//Three numbers are divided c program...
#include <stdio.h>
int main(){
    int X, Y, Z, result;
    X = 120, Y = 2, Z = 3;
    result = X / Y / Z;
    printf("The result is: %d", result);
    return 0;
}
