#include <stdio.h>
int main(){
    int number;
    if( 10 != 10){
        printf("Value of number: %d", number);
    }
    else{
        printf("hello programmer: %d", number);
    }
    return 0;
}
