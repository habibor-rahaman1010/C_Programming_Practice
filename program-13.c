//user tow number input c program, and sumations the this value..
#include <stdio.h>
int main(){
    int x, y, sum;
    printf("place input your value X:");
    scanf("%d", &x);
    printf("place input your value Y:");
    scanf("%d", &y);
    sum = x + y;
    printf("The result is: %d", sum);

    return 0;
}
